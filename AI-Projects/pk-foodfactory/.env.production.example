# Copy values into GitHub Actions secret VITE_API_URL (Settings → Secrets → Actions).
# Do not commit real secrets. Production builds bake this URL into the JS bundle.
#
# Use your API CloudFront HTTPS URL (not the raw Elastic Beanstalk http:// URL).
VITE_API_URL=https://d3cvs28uau3ofy.cloudfront.net/api
