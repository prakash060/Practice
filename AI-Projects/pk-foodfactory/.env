# Frontend Environment Variables

# Backend API: leave empty in dev to use Vite proxy (see vite.config.ts → /api → port 5000).
# For production build, set the full URL, e.g. https://your-api.com/api
VITE_API_URL=