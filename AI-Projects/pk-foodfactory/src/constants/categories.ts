/**
 * Categories are admin-managed (see `state/FoodContext`). Defaults for emoji
 * and accent when a category has not set them yet.
 */
export const DEFAULT_CATEGORY_EMOJI = '🍽️'
export const DEFAULT_CATEGORY_ACCENT = '#6b5ef7'
